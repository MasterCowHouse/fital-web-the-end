import { Box } from "@mui/material";
import Header from "./Header";

export default function PageCompraDeTokens () {
    return (
        <Box sx={{
            color: 'white'
        }}>
            <Header />
        </Box>
    )
}